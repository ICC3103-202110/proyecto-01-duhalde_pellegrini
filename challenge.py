from player import Player

class Challenge:
    def __init__(self):
        self.challenge = challenge()

    def challenge(self):
        Player.show_card()
        if Player.show_card() == "carta jugada (no sé como llamar a esa carta)"
            return True
        else :
            return False
